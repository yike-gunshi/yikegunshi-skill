# 《XXX Skill 设计文档》

> 阶段：S2 设计 · 状态：待评审
> 读者：业务方确认边界、研发确认 tool、评测同学确认考点
> 档位：轻档 / 完整档（轻档只填 2.1–2.4）

## 0. Skill 化决策（S0 结论）

为什么这个需求需要一个独立的 skill，而不是扩展已有 skill / 加一个 tool / 改 prompt / 不做：

<一段话，说清替代方案和它们的代价>

满足的资格条件：☐ 会重复使用 ☐ 容易被路由错 ☐ 有脚本能省重复劳动 ☐ 需要治理

## 1. 收料结论（S1）

### 1.1 需求与场景

- 使用者与处境：
- 现在怎么解决、哪一步最烦：
- 理想产出长什么样：
- 什么算做对了：

### 1.2 已有决策与约束

| 类别 | 内容 | 可否推翻 |
|---|---|---|
| 平台与形态 | | |
| 上下游 | | |
| 合规与安全 | | |

参考对象（≤5）及**不借鉴什么**：

| 参考 | 借鉴什么 | 明确不借鉴什么 |
|---|---|---|

### 1.3 基建盘点（完整档）

**tool 清单**

| tool | 什么时候调 | 必调/看情况 | 失败时降级到 |
|---|---|---|---|

**字段表**（每个 tool 一张）

| 字段 | 类型 | 本 skill 怎么用 | 用不上（写原因） |
|---|---|---|---|

## 2. 设计

### 2.1 要解决的问题

一句话：这个 skill 替用户完成了什么他原本要自己做的事。

### 2.2 定位与边界

**定位**：<一句话说它是什么形态的产出> + <像什么角色> —— <把什么讲透>

**核心立场**：<这个场景里用户真正要的是什么> 所以 <产出的组织方式跟着变成什么>

**范围**：<给定什么输入，处理什么>。一次只处理 <粒度>。

**不处理**：

| 不处理什么 | 交给谁 |
|---|---|

**≥3 个"很像但不该触发"的 query**（门禁第 1 条）：

1.
2.
3.

### 2.3 触发与意图路由

**触发层素材**（打磨留到 S3）

- 应触发的 query 例句：
- near-miss 负例：
- 可能抢活的隔壁 skill：

**路由层分流**

| 意图强度 | 判断信号（要可判断，不能是"看起来很想要"） | 应答粒度 |
|---|---|---|
| 强 | | 完整答案 |
| 弱 | | 简答 |
| 潜在 | | 简答 + 反问 |

### 2.4 输出框架

**固定骨架**（锁死顺序，任何 query 都按此结构，不走动态逻辑）

1.
2.
3.

**必出模块**

| 模块 | 入选条件 | 内部结构 | 对应考点 |
|---|---|---|---|

**触发式模块**（命中才出）

| 模块 | 触发条件 | 命中多个时怎么取舍 |
|---|---|---|

> 门禁第 3 条：每个必出模块都要能对应到一条考点。对不上就删掉或补考点。
> 模块总数 ______ 个。超过 8 个先问"哪几个能合并"。

### 2.5 执行流程

| 步 | 做什么 | 调什么 tool | 输入 | 输出 | 失败怎么办 |
|---|---|---|---|---|---|
| 1 | | | | | |

背景 / 角色 / 场景补充说明：

### 2.6 辅助资源

| 资源 | 类型 | 为什么需要 |
|---|---|---|
| | scripts/ | 每次都要重写同样的代码 |
| | references/ | 需要查阅但不必每次加载 |
| | assets/ | 会被复制进最终产出 |

### 2.7 输入输出定义

**预期输入**：场景 + query 形态

**≥5 条真实的、带细节的 query 样本**（门禁第 5 条，要长得像真人打的字）：

1.
2.
3.
4.
5.

**预期输出**：内容 + 形态

### 2.8 上下文分层规划

| 层 | 放什么 | 预估体量 |
|---|---|---|
| description | | 目标 ~300 字符 |
| SKILL.md 正文 | | 目标 < 500 行 |
| references/ | | 每份要在 SKILL.md 里点名何时读 |
| scripts/ | | |
| assets/ | | |

## 3. 完备性门禁自查

| # | 检查项 | 通过 |
|---|---|---|
| 1 | 能说出 ≥3 个"很像但不该触发"的 query | ☐ |
| 2 | 能说清不处理什么、该交给谁 | ☐ |
| 3 | 每个必出模块都对应一条考点（轻档豁免） | ☐ |
| 4 | 每个 tool 字段已确认，标明哪些不用（轻档豁免） | ☐ |
| 5 | 有 ≥5 条真实的、带细节的 query 样本 | ☐ |
| 6 | 能说清失败怎么办（工具挂了、数据不足） | ☐ |

**任何一条不通过，回 S1 继续澄清，不进 S3。**

## 4. 待决策项

| # | 问题 | 我的倾向 | 状态 |
|---|---|---|---|
