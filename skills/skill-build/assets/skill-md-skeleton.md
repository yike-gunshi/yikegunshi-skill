---
name: skill-name-in-hyphen-case
description: <做什么，一句话>。<什么时候用它——覆盖同义说法、覆盖用户描述症状但没说方案的情形、加"哪怕用户没明说 X">。<改已有的也走它：列出典型的修改场景>。不处理：<排除项>，那些走 <该交给谁>。
---

<!--
写完删掉这段注释。

description 自查：
- 所有"何时使用"的信息都在这里，正文里没有 When to Use 章节
- 执行层禁令不在这里（它们不影响触发决策），已移回正文
- 有排除项
- ≤1024 字符，目标 ~300
- 没有尖括号 < >

正文自查：
- < 500 行，超了外移到 references/
- 强调词不密集，能解释 why 的地方不堆 MUST
- 红线 ≤ 5 条，且每条都是"违反了收不回来"
- 每个 references/ 文件都在正文里被点名"什么时候读"
- 没有 README / CHANGELOG / INSTALLATION_GUIDE
-->

# skill-name — 一句话定位

<交付什么。一两句。>

## 定位与边界

<定位：是什么形态的产出 + 像什么角色 + 把什么讲透>

<核心立场：这个场景里用户真正要的是什么，所以产出的组织方式跟着变成什么>

边界之外的交出去：<不处理 X，交给 Y>；<不处理 Z，交给 W>。

## 按需读取

每个文件在对应步骤读，用完即弃：

| 文件 | 什么时候读 |
|---|---|
| `references/xxx.md` | S? 做什么之前 |

## 意图分流

| 意图 | 信号 | 应答 |
|---|---|---|
| 强 | | 完整答案，走全流程 |
| 弱 | | 简答，给骨架不铺细节 |
| 潜在 | | 简答 + 反问确认 |

## 工具

<每个 tool：什么时候调、入参怎么构造、返回哪些字段有用、哪些字段本 skill 不使用、失败降级到什么。字段全表外移到 references/tools.md。>

## S1 <第一步在干什么>

<祈使句。解释 why，不堆 MUST。>

## S2 <第二步>

## S3 <第三步>

## 输出

**固定骨架**（顺序锁死）：

```
<完整骨架，标清哪些是必出、哪些是命中才出>
```

**必出模块**：<每个模块的入选条件和内部结构>

**触发式模块**：<触发条件，命中多个时的取舍规则>

## 红线

<只留"违反会造成不可逆外部损害"的，≤5 条。格式和风格约束改写成上面各节里带 why 的说明。>

1.
2.

## 边界与失败处理

<工具挂了、数据不足、query 超出范围时分别怎么办。>
